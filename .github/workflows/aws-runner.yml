name: AWS Runner
run-name: ${{ github.ref_name }}-${{ github.run_number }}

on:
  workflow_dispatch:
  workflow_call:
    secrets:
      AWS_GITHUB_ACCESS_KEY_ID:
        required: true
      AWS_GITHUB_SECRET_ACCESS_KEY_ID:
        required: true
      AWS_GITHUB_RUNNER_TOKEN:
        required: true     
        
defaults:
  run:
    shell: bash
    # runs-on:
    #   group: Build 
    #   labels: ubuntu

env:
  AWS_REGION: ap-southeast-2                   # set this to your preferred AWS region, e.g. us-west-1
  build-artifact-key: app-${{ github.sha }}
  ECR_REPOSITORY: java-application           # set this to your Amazon ECR repository name

jobs:
  start-runner:
    name: Start self-hosted EC2 runner
    runs-on: ubuntu-latest
    outputs:
      label: ${{ steps.start-ec2-runner.outputs.label }}
      ec2-instance-id: ${{ steps.start-ec2-runner.outputs.ec2-instance-id }}
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_GITHUB_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_GITHUB_SECRET_ACCESS_KEY_ID }}
          aws-region: 'ap-southeast-2'
      - name: Start EC2 runner
        id: start-ec2-runner
        uses: machulav/ec2-github-runner@v2
        with:
          mode: start
          github-token: ${{ secrets.AWS_GITHUB_RUNNER_TOKEN }}
          ec2-image-id: ami-040585fab7b70b6c7
          ec2-instance-type: t3.medium
          subnet-id: subnet-0b38821d605af3a64
          security-group-id: sg-004bf88b5a89e74ea
          iam-role-name: arn:aws:iam::865983312994:instance-profile/github-actions-role
          use-jit: true
          aws-resource-tags: > # optional, requires additional permissions
            [
              {"Key": "Name", "Value": "ec2-github-runner"},
              {"Key": "GitHubRepository", "Value": "${{ github.repository }}"}
            ]
          block-device-mappings: > # optional, to customize EBS volumes
            [
              {"DeviceName": "/dev/sda1", "Ebs": {"VolumeSize": 30, "VolumeType": "gp3"}}
            ]
    # start-runner:
    #     timeout-minutes: 5              # normally it only takes 1-2 minutes
    #     name: Start self-hosted EC2 runner   
    #     runs-on: ubuntu-latest   
    #     steps:      
    #       - name: Start EC2 runner
    #         id: start-ec2-runner
    #         uses: unblocked/ec2-action-builder@v1.11
    #         with:
    #           github_token: ${{ secrets.AWS_GITHUB_RUNNER_TOKEN }}
    #           aws_access_key_id: ${{ secrets.AWS_GITHUB_ACCESS_KEY_ID }}
    #           aws_secret_access_key: ${{ secrets.AWS_GITHUB_SECRET_ACCESS_KEY_ID }}
    #           aws_region: "ap-southeast-2"
    #           ec2_instance_type: t3.medium
    #           ec2_ami_id: 'ami-06259b63260eddc13'
    #           aws_iam_role_arn: "arn:aws:iam::865983312994:role/github-actions-role"
    #           ec2_subnet_id: "subnet-0b38821d605af3a64"
    #           ec2_security_group_id: "sg-004bf88b5a89e74ea"
    #           ec2_instance_ttl: 5                # Optional (default is 60 minutes)
    #           ec2_spot_instance_strategy: MaxPerformance    # Other options are: SpotOnly, BestEffort, MaxPerformance 
    #           ec2_instance_tags: >                          # Required for IAM role resource permission scoping
    #                           [
    #                             {"Key": "Owner", "Value": "Github-Runner"}
    #                           ]
    # start-runner:
    #   runs-on: ubuntu-latest
    #   steps:
    #     - id: deploy
    #       uses: bitovi/github-actions-deploy-github-runner-to-ec2@v0.1.4
    #       with:
    #         aws_access_key_id: ${{ secrets.AWS_GITHUB_ACCESS_KEY_ID }}
    #         aws_secret_access_key: ${{ secrets.AWS_GITHUB_SECRET_ACCESS_KEY_ID }}
    #         aws_default_region: ap-southeast-2
    #         additional_tags: '{\"Name\":\"GitHub-Runner\",\"key2\":\"value2\"}'
    #         aws_ec2_ami_id: 'ami-040585fab7b70b6c7'
    #         aws_ec2_instance_root_vol_size: 30
    #         aws_ec2_iam_instance_profile: github-actions-role
    #         aws_ec2_security_group_name: "GitHub-Runner" 

    #         aws_vpc_id: vpc-0cc923593f8d9050f
    #         aws_vpc_subnet_id: subnet-0b38821d605af3a64

    #         ec2_instance_type: t3.medium
    #         ec2_instance_public_ip: false
    #         ec2_additional_tags: '{\"key3\":\"value3\",\"key4\":\"value4\"}'
            
    #         repo_url: https://github.com/frankbuttigieg1993-gmail/java_application
    #         repo_access_token: ${{ secrets.AWS_GITHUB_RUNNER_TOKEN }}

    #         stack_destroy: false
    #         tf_state_bucket_destroy: true
    #         tf_state_bucket: terraform-frankbuttigieg1993-github-runners
  do-the-job:
    name: Do the job on the runner
    needs: start-runner # required to start the main job when the runner is ready
    runs-on: ${{ needs.start-runner.outputs.label }} # run the job on the newly created runner
    steps:
      - name: Hello World
        run: echo 'Hello World!'